"""
tsc_gnn — TSC-GNN 深核（Phase 2）。

时间×状态条件化图神经网络：在推断的 GRN（基因调控图）上做消息传递，
预测虚拟扰动（KO）的下游表达效应 Δ。纯 NumPy 实现（无 torch 依赖，
完全可复现、可审计）；图的深核工作在状态条件化边权重 + K 步消息传递，
读头为 Ridge 回归。

设计要点（详见 README §9）：
- GRN = 真实卒中数据推断的对称 k-NN 相关图（每基因出边齐全，可对任意基因做 KO）。
- 边权重按连续细胞状态做门控（state-gated）：炎症态下部分边增强/抑制。
- 扰动 p（KO 某基因）作为节点特征注入，消息传递沿状态条件化图传播 → 节点
  嵌入携带 A p / A² p（扰动的图传播），读头（Ridge）据此预测 Δ。
- 扁平 baseline（Ahlmann-Eltze 式）只看到 [X, p, state, time]，无图传播 →
  对"未见过的扰动基因"无法泛化，TSC-GNN 因显式图传播而能。
"""
