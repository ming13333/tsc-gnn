# 全流程 Bug 审计 · Methods/Results 初稿对照

> 审计模式：Retrospective Audit（对照 `Methods_Results_初稿_2026-07-09.md` 审查 A→B→C→D 全代码与产物）。
> 审计日期：2026-07-09 ｜ 审计依据：`research-bug-audit` 分类图谱（A1–B3）+ 红 flag 扫描 + 生物学常识校验。

---

## 0. 审计范围与已验证的"健康项"（先讲结论）

**总体结论：未发现会推翻任一结论的统计/算法致命 bug。** 代码实现了之前 Bug Audit 的 5 条阻断级修复（p 分辨率、双重 FDR、PC 组成校正、方向标签、manifest），且：

| 初稿声明 | 核查方式 | 结论 |
|---|---|---|
| L1 TF rank ρ=0.517 / 0.548 / 0.482, p<1e-15 | 核对 `cc_diag.log` | ✅ 数字真实，完全一致 |
| 边级不重现（方向一致≈0.52、r(dW)≈0.10、Jaccard≈0） | 核对 `cc_diag.log` Part 3 | ✅ 真实 |
| L4 robust 命中 4 药、置换 real=4 vs 随机 2.75±1.37、emp_p=0.333 | 核对 `drug_perm_result.json` | ✅ 数字真实 |
| 三队列共通 Top20 仅 Sox10 | 核对 `cc_diag.log` Part 1 | ✅ 真实（"full∩c1∩c2=['Sox10']"） |
| 8–36 边/转移 @ pooled q<0.05 | 核对 `REWIRING_报告_v3` 汇总表 | ✅ 计数=8/28/19/36，吻合 |
| 三处运行协议一致（n_perm=200/n_pc=10/aff_q=0.5/seed 0,1,2） | 比对 `run_rewiring.py` vs `run_rewiring_single.py` | ✅ 完全一致 |
| 种子完整（rewiring 0/1/2；D 置换 2026） | Grep `seed|random` 全 py | ✅ 均已固定 |
| 分隔符均显式指定（sep="\t"/","） | Grep `sep=|read_csv` | ✅ 无分隔符歧义 bug |

**已正确捕获的致命 bug（正面清单，证明审计纪律生效）：**
1. **D 置换重复计数假象**（unique 计数前 emp_p=0.048 → 纠正后 0.33）。这是最危险的确证偏误，已在写文章前捕获并改写结论。
2. **vorinostat "6 细胞系一致"背景误判**：随机签名下出现率 53%、max n_hits=16，证明非特异信号。已捕获并降级。
3. **`cc_diag.py` merge 后缀 bug**（`m[col_a]`→`m[col_a+"_a"]`）：已修复。
4. **整合集 vs 单队列"同 transition"循环比较**：已识别基线混合，改为 TF 级跨队列比较。

---

## 1. 分级审计报告

| 序号 | 严重度 | 类型 | 位置（文件:行） | 现象 / 红 flag | 对结论的潜在影响 | 修复建议 |
|---|---|---|---|---|---|---|
| **1** | 🚫 阻断级 | 声明与数据不符（诚信） | `Methods_Results_初稿` §3.6 L132, L136 | 草稿称签名="inflammatory axis up, **myelin-repair axis down**"（引 §3.4–3.5），且 HDAC 抑制剂"align exactly with the inflammatory **and myelin-regeneration** axes"。**实测 `disease_signature.json` 的 down 集不含任何髓鞘/修复结构基因**（PLP1/MBP/MAG/MOG/CNP/OLIG 全无），实为 IFIT/ISG 干扰素基因、TMEM119/CST3 小胶标记、CAV1/2 等异质低表达基因。"myelin-repair down"在 24h 签名中**不存在**。 | 审稿人一查签名即发现"down 集无髓鞘基因"→ 翻译叙事的"pro-myelin"链接失据，涉嫌 overclaim。 | 见 §2 替换文本：将"签名=炎症 up + 异质 down"与"L2/L3 独立识别的髓鞘再生程序"严格分开，pro-myelin 链接只归因于 L2/L3，不归因于签名。 |
| **2** | ⚠ 重要级 | 未披露范围限制 | `build_disease_signature.py` L21 + `build_cache_single.py` L171–173；草稿 §3.6 / §2.2 | 疾病签名从 `gse174_cache.npz` 构建，而该 cache 在 `build_cache_single.py` 中已**子集到 DoRothEA ABC 基因**（8736 个，仅网络基因）。故 LINCS 反向匹配签名**仅含调控网络基因，非全转录组差异表达**。草稿未披露此限制。 | 签名偏向 TF/靶基因，可能遗漏强卒中标记；审稿人问"为何不用全 DE"需有答。 | 草稿明示："signature was derived from the DoRothEA-network gene space (the same space as the rewiring), i.e. a regulatory-program-filtered DE, not the full transcriptome." 并列为 L4 局限。 |
| **3** | ⚠ 重要级 | 解释精度 | `cc_diag.py` L46 vs `run_rewiring.py` L36；草稿 §3.3 | 整合集 `s_full` 的 TF 排名**跨 4 个转移取 max｜ΔW｜**，其中包含 2 个**跨队列拼接伪转移**（24h→2d = GSE174574 24h vs GSE225948 2d；sham→14d = 174 sham vs 225 14d）。与单队列（c1 仅 sham→24h 真实转移）比较时，ρ 混合了拼接伪转移信号。Sox10 三队列共通是干净信号，但 ρ 的"跨队列重现"解释需加限定。 | ρ≈0.5 结论成立，但"跨队列重现"措辞略过强；精确表述应为"TF 主调控因子在可用转移上的排名总体重现"。 | 草稿 §3.3 加一句：integrated ranking pools all four transitions, two of which are stitched cross-cohort pseudo-transitions; the ρ therefore reflects TFs that drive rewiring in general, with Sox10 the clean cross-cohort-reproduced exemplar. |
| **4** | ⚠ 重要级 | 统计精度 | `drug_reversal_permutation.py` L22, L64–84；草稿 §3.6 | emp_p=0.33 的**基因抽样**种子固定（2026），但 **LINCS API 响应是实时的、不可复现**；且 N=20 单次估计，perm_std=1.37 → 重跑可能得 emp_p≈0.1–0.8。草稿把 0.333 当精确值报。 | 结论（不显著）稳健，但精确 p 值不应显得确定。 | 草稿改为"empirical p ≈ 0.3 (N=20, single-shot, live API)"，并说明不显著结论对重跑稳健；或加大 N 并重跑。 |
| **5** | 💡 建议级 | 静默数据丢失 | `build_cache_single.py` L89–99 | GSE174574 重复 gene symbol 被重命名为 `Sox10-1/-2`，随后因不匹配 DoRothEA 被静默丢弃（65 个）。影响≈0（这些基因本不进网络），但是代码气味。 | 极低；不影响结论。 | 改用 `var_names_make_unique` 风格或显式去重+告警；或确认 10x 无重复 symbol。 |
| **6** | 💡 建议级 | 正交映射脆弱 | `human_module_enrich.py` L48–50 | 鼠→人 TF 正交靠 `manual` 字典 + `.upper()`，非 1:1 时易错（如鼠 Sox2→SOX2 正确，但其它旁系同源未覆盖）；未命中字典者直接 upper，可能错配。 | 当前 12 个手策 TF 均 1:1 正确，影响低；扩展性差。 | 引入正式正交表（biomart/orthology DB）做 1:1 映射，缺失显式记录。 |
| **7** | 💡 建议级 | 潜在崩溃 | `human_module_enrich.py` L86–102 | 若某 TF 的靶数匹配 null 集 `cand` 为空，`random.choice(cand)` 抛 IndexError。当前未触发（脚本跑完）。 | 当前无碍；换 TF 时可能崩。 | 加 `if not cand: continue` 守卫。 |
| **8** | 💡 建议级 | 静默去重 | `build_cache_single.py` L114–143 | GSE225948 用 `df.index` 直接取基因，未做 make_unique；重复基因行在 inner-join 时只保留首个（`g.index(c)` 取第一处）。 | 低；若 counts.csv 有重复行会静默丢。 | 加载时检查 `df.index.duplicated()` 并告警/聚合。 |

---

## 2. 🚫 阻断级修复 · 直接替换文本（ISSUE 1）

**原文 §3.6（L132）：**
> "…converted the stroke injury program (inflammatory axis up, myelin-repair axis down, from §3.4–3.5) into a disease signature…"

**改为：**
> "…converted the 24 h stroke injury program into a disease signature — up-regulated injury/inflammatory genes (SPP1, CCL4, LGALS3, CD14, C5AR1, TNF …) and a heterogeneous set of lower-expression genes (interferon-response, microglia-marker and transport genes; see `disease_signature.json`). Notably, the 24 h signature is dominated by the **acute inflammatory/injury axis** and does **not** by itself contain down-regulated myelin/repair structural genes; the myelin-regeneration axis is recovered independently by the rewiring analysis (Levels 2–3), not by this signature."

**原文 §3.6（L136）：**
> "The robust signature returned four literature-supported neuroprotective/anti-inflammatory agents in the top 50: vorinostat and trichostatin A (HDAC inhibitors) and mevastatin and rosuvastatin (statins) — mechanistic classes that align exactly with the inflammatory and myelin-regeneration axes recovered in Levels 2–3."

**改为：**
> "The robust signature returned four literature-supported agents in the top 50: vorinostat and trichostatin A (HDAC inhibitors) and mevastatin and rosuvastatin (statins). Their **anti-inflammatory** relevance aligns directly with the inflammatory axis captured in the 24 h signature; their **pro-myelin / neuroprotective** relevance aligns with the remyelination program independently recovered by the rewiring analysis (Sox10→Plp1, Level 2–3) — a cross-level convergence, not a within-signature effect."

**原文 §3.6（L140 局限清单）新增一条：**
> "- The 24 h disease signature was derived from the DoRothEA-network gene space (the same space as the rewiring), i.e. a regulatory-program-filtered differential expression, not the full transcriptome; this may omit non-network stroke markers and should be extended with a full-transcriptome signature in future work."

---

## 3. ⚠ 重要级修复要点（ISSUE 2–4）

- **ISSUE 2**：§3.3 在 ρ 表后补一句（见上表建议文本），明确 integrated ranking 跨 4 转移、含 2 个拼接伪转移；ρ 反映"总体驱动 rewiring 的 TF"，Sox10 是干净的跨队列重现范例。
- **ISSUE 3**：§3.6 把 "empirical p = 0.33" 改为 "empirical p ≈ 0.3 (N=20, single-shot, live-API; the non-significant conclusion is robust to re-runs)"。如时间允许，把 N_PERM 提到 100 并重跑，给出更稳的估计。
- **ISSUE 4（与 ISSUE 2 同源）**：§2.2 已披露拼接与 batch×time 混淆，但 §3.1 列出四转移时未重申"其中 24h→2d 与 sham→14d 为跨队列拼接"。在 §3.1 开头加一句即可。

---

## 4. 生物学常识校验（终极过滤器）

- ✅ UP 签名 = SPP1/CCL4/LGALS3/TNF/CD14/MSR1/LPL… = 小胶/巨噬细胞激活 + 先天免疫。符合卒中 24h 急性损伤常识。
- ✅ rewiring 主信号 Sox10/Sox2/Sox9（少突髓鞘）→ Plp1/Mbp，符合修复期髓鞘再生常识。
- ✅ Cebpb→Il1b 炎症消退，符合急性期炎症程序常识。
- ✅ HDAC 抑制剂/他汀为已知神经保护/抗炎药，与炎症轴方向一致（常识自洽）。
- ⚠ **DOWN 签名无髓鞘基因** = 异常但可解释（24h 急性期主导炎症/胶质激活，脱髓鞘更晚），已据此修正 ISSUE 1，**非代码 bug，是表述 bug**。
- ✅ 阳性对照存在：Fos（即刻早期应激伪影）在严格阈值下三队列共通 → 被正确作为负向对照呈现。阴性对照：随机签名置换（D 步）与靶数匹配置换（C2 步）均到位。

---

## 5. 🚫 投稿前必须修复清单（阻断级）

- [ ] **ISSUE 1**：修正 §3.6 两处措辞（L132、L136）+ 局限清单加 DoRothEA 基因空间限制。签名 down 集不含髓鞘基因，pro-myelin 链接只能归因 L2/L3。
- [ ] 重读 §3.6 全文，确保"签名内容"与"L2/L3 内容"不互相冒充（其余句子若也有类似措辞一并改）。

## 6. 💡 改进建议（重要级 + 建议级）

- [ ] ISSUE 2/4：§3.1、§3.3 显式标注整合集的"拼接伪转移"，精确化 L1 解释。
- [ ] ISSUE 3：emp_p 表述软化或加大 N 重跑。
- [ ] ISSUE 5/8：cache 构建加重复基因 symbol 检查+告警。
- [ ] ISSUE 6：鼠→人正交改正式映射表。
- [ ] ISSUE 7：`human_module_enrich.py` 加 `cand` 空守卫。
- [ ] 可选增强：用全转录组 cache 重跑 D 签名（解除 ISSUE 2 限制），看命中是否更特异。

---

*审计结论一句话：代码与统计无致命 bug，六条作者红线基本守住；唯一必须动笔修的是 §3.6 对 D 签名"down-regulated repair / myelin"的 overclaim（阻断级），其余为解释精度与未披露范围（重要级）。整体已达可进入英文撰写的状态。*
